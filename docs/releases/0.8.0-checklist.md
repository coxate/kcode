# Kcode 0.8.0 集成 Checklist

> 状态：已批准。此清单不替代 Worktree M1 或 Agent Team M2 各自的验收；它只在两者均通过后检查同一版本中的组合行为、发布资料与可恢复性。

## 前置里程碑

- [ ] **M1 Worktree 已验收：**`docs/worktrees/checklist.md` 的核心行为、安全删除、SubAgent 隔离和质量门禁有实际证据，当前分支未引入回归。（验证：复跑 Worktree 定向测试与真实 Git clean/dirty 场景）
- [ ] **M2 Agent Team 已验收：**`docs/agent-teams/checklist.md` 的 AC1–AC13 与端到端场景全部有实际证据。（验证：审阅 Checklist 证据并复跑 Team 定向组）
- [ ] **范围未膨胀：**发布中不存在 tmux/iTerm2、跨进程邮箱、持久化 Team、Coordinator Mode、自动唤醒 Lead、自动提交/合并或强制删除。（验证：用户文档、工具/命令清单与行为测试对照）

## M1 + M2 组合行为

- [ ] **默认安全组合：**旧 0.7.0 配置可直接启动；Worktree 手动命令可用，Team 九工具稳定注册但默认 disabled，不产生额外模型费用。（验证：旧配置启动、工具清单与调用计数）
- [ ] **一个 Manager 事实源：**手动 Worktree、普通隔离 SubAgent 和 Team 成员的 Git 列表/元数据可以对账，名称、owner 和 kind 不互相认领或误清理。（验证：同一真实仓库创建三类项目后 list/status/finalize）
- [ ] **主目录保持不变：**两个 Team 成员与一个普通隔离 SubAgent 同时工作时，各自路径/分支唯一，主 Agent cwd、主分支和主目录同名文件不变。（验证：并发修改同名文件并比较四处文件与 Git HEAD）
- [ ] **全局容量一致：**普通 SubAgent 与 Team 成员共同受一个 running/retained 上限；idle Team 成员释放运行槽但占 retained/Team 名额，任一拒绝都不留下 Worktree。（验证：混合任务填满两个上限）
- [ ] **审批不分叉：**普通 SubAgent 与 Team 成员的后台写入都经过同一审批队列，来源清晰，取消/退出能关闭全部等待项。（验证：同时制造两类审批并逐一处理）
- [ ] **成果报告一致：**普通隔离任务和 Team 成员都报告 path、branch、base、HEAD、dirty、kept、reason，并使用相同的脱敏、32 KiB 与 fail-closed 语义。（验证：两类长结果和 Git 故障场景对照）
- [ ] **无成果才清理：**普通任务完成、Team stop/delete、App 退出仅清理可证明无成果的自动 Worktree；手动分支和所有有成果/未知状态项保留。（验证：三类 Worktree 的 clean/dirty/new commit 矩阵）
- [ ] **没有自动 Git 汇合：**整个组合流程中不自动 commit、merge、cherry-pick、rebase 或 force remove；成果只能从报告路径人工 review。（验证：Git 命令记录与主分支前后对比）
- [ ] **退出顺序可靠：**退出先停止 Team，再关闭剩余 TaskManager，随后关闭 Hooks/Session/Memory/MCP；没有运行 Agent、Git 子进程或悬挂审批。（验证：混合运行+审批退出集成测试）
- [ ] **重启边界清晰：**重启后 Team/邮箱/任务板不恢复，普通会话持久化仍按旧规则工作；保留的成果 Worktree和分支仍可用 Git review。（验证：退出、重启、session resume 与 Git list）

## 用户界面与文档一致性

- [ ] **帮助可发现：**`/help` 同时列出 `/worktree` 和 `/team`；各自详细帮助与真实参数数目、名称和错误用法一致。（验证：真实 App 执行 help 与命令测试）
- [ ] **状态分层：**普通 `/status` 保持兼容，`/team status` 才显示 Team 明细，`/worktree list/status` 继续显示 Git 隔离详情；关闭 Team 时旧状态输出不变化。（验证：enabled on/off 输出快照）
- [ ] **配置示例可运行：**`config.example.yaml` 包含 `teams.enabled: false`、`max_members: 3`，复制为用户配置后能解析；项目配置不能开启 Team。（验证：对示例运行 load_config 和项目覆盖测试）
- [ ] **README 边界完整：**文档说明 Git 前置条件、相邻目录、默认 Worktree、shared 风险、并行费用、in-process、重启不恢复、成果 review 和不自动合并。（验证：文档条目逐项与实现对照）
- [ ] **初学者操作路径：**README 给出从启用 Team、创建/查看/停止/删除，到打开保留 Worktree review 的最小安全示例，并明确哪些操作可撤销、哪些可能产生费用。（验证：按示例在临时仓库逐条执行）

## 版本与构建产物

- [ ] **版本统一：**`src/kcode/__init__.py`、`pyproject.toml`、`kcode --version` 与 wheel metadata 全部为 `0.8.0`。（验证：源码读取、CLI 输出和解包 wheel metadata）
- [ ] **当前文档无旧版本漂移：**README、配置示例和当前帮助不再把 0.7.0 当成当前版本；历史 Spec/Plan 中的基线叙述可以保留。（验证：全仓搜索 `0.7.0` 后逐项分类）
- [ ] **Wheel 包含新模块：**构建 wheel 后包含完整 `kcode.worktrees`、`kcode.teams` 及既有内置 Agent/Skill/Hook 资源，不包含测试缓存、临时 Worktree 元数据或本机配置。（验证：列出 wheel 文件清单）
- [ ] **安装后冷启动：**在全新临时虚拟环境安装 wheel，复制配置示例后 `kcode --version`、`/help` 或等价 Registry smoke 可工作。（验证：离线安装本地 wheel 并运行 smoke）
- [ ] **发布目录干净：**构建与测试没有污染主仓库、提交 `.metadata.json`、`.env`、凭据、缓存或相邻成果 Worktree。（验证：`git status --short`、敏感文件扫描和管理目录核对）

## 自动化质量门禁

- [ ] **完整测试：**`uv run pytest` 退出码为 0，并记录 passed/skipped 数；任何 skip 都有既有、明确原因。（验证：保存完整 pytest 摘要）
- [ ] **Lint：**`uv run ruff check .` 退出码为 0。（验证：保存命令输出）
- [ ] **格式：**`uv run ruff format --check src tests` 退出码为 0。（验证：保存命令输出）
- [ ] **Diff 健康：**`git diff --check` 退出码为 0，无空白错误、冲突标记或意外二进制文件。（验证：命令输出和 diff stat）
- [ ] **定向稳定性：**Worktree、Team、SubAgent、Permission、Hook、Command、Config、CLI、App 定向测试可单独重复通过，测试之间无顺序依赖。（验证：分组运行后再乱序/重复关键并发测试）
- [ ] **并发稳定性：**idle 唤醒竞态、并发 spawn、并发同名文件修改和三成员退出关键测试重复运行，无偶发双启动、消息滞留或清理越界。（验证：关键测试循环运行并记录次数）

## 最终人工端到端

- [ ] **发布候选 E2E：**在干净真实 Git 仓库从 0.8.0 wheel 启动 → 确认 Team 默认关闭 → 用户级开启 → 创建 Team 与两个默认成员 → 建立依赖任务并互发消息 → 观察 idle 续派与审批 → 两人修改同名文件 → stop/delete → 主目录未变且成果路径可 review。（验证：保存用户可见输出、Git 状态和路径）
- [ ] **退出恢复 E2E：**在成员运行/等待审批时退出 → 无残留进程 → 重启后 Team 不恢复 → 保留 Worktree仍可读取 → 手动 Worktree命令和普通 SubAgent仍正常。（验证：进程、Registry、Git 和文件检查）
- [ ] **失败演练 E2E：**制造 dirty 主目录、非 Git 默认 spawn、Git status 失败和 delete 时成员运行四种错误，系统全部明确拒绝或保留现场，没有费用偷跑、强删或主分支变化。（验证：四种真实/故障注入场景前后对比）

## 发布结论

- [ ] **证据齐全：**以上条目均有命令输出、自动测试或真实场景证据，不使用推测作为通过依据。（验证：逐项检查证据字段、命令退出码和场景记录均非空）
- [ ] **无未解释阻塞：**任何失败都已修复并重验，或明确决定延期且从 0.8.0 发布范围移除。（验证：失败记录均链接到重验结果或明确的范围变更）
- [ ] **可以发布 0.8.0：**M1、M2、组合 E2E、构建和质量门禁全部通过后，才将发布结论标记完成。（验证：三份 Checklist 无未完成项，最终 wheel smoke 再次通过）
