# KCode Agent Loop Checklist

## 实现完整性

- [ ] 普通请求可经历至少三轮工具并自动给出最终文本。（假 Provider 集成测试）
- [ ] 文本/思考实时显示，完整响应能还原工具、停止原因、连续状态和 usage。（交错流测试）
- [ ] 每轮有模式、轮次、阶段进度，并正确报告本次与累计 Token。（事件断言）
- [ ] 六种停止原因唯一，停止后不再请求 Provider。（参数化测试）
- [ ] 工具错误、拒绝和参数错误可回灌并继续调整。（失败恢复测试）

## 并发与安全

- [ ] 同批只读工具重叠且不超过上限，结果按调用索引排列。（并发计数测试）
- [ ] 副作用工具互不重叠并形成前后批次屏障。（时间线测试）
- [ ] Plan Mode 仅允许读工具和白名单命令，其他调用不弹确认框。（策略集成测试）
- [ ] Do Mode 保留既有确认；拒绝后无副作用。（Textual pilot）
- [ ] Ctrl+C 可停止流、授权、并发工具和命令并清理进程。（取消测试）

## 状态与协议

- [ ] 异常停止保留完整检查点并丢弃当前残片。（历史测试）
- [ ] `/plan`、`/do`、`/clear` 的模式和一次性计划行为正确。（Session/TUI 测试）
- [ ] OpenAI/DeepSeek 多工具、usage 与 reasoning 连续状态正确。（假 SDK 测试）
- [ ] Anthropic 多工具、usage、thinking、redacted thinking 和签名正确。（假 SDK 测试）
- [ ] 缺失 usage 显示未知且不污染累计值。（缺失数据测试）

## 兼容与端到端

- [ ] 旧 YAML 以 10/4 默认值启动，覆盖和越界行为正确。（配置测试）
- [ ] 六工具、安全策略、普通聊天和原命令不退化。（原测试套件）
- [ ] `uv lock --check`、`compileall` 和完整 pytest 全部通过。
- [ ] tmux 离线 TUI 完成多轮、并发、Plan/Do 和取消。（终端捕获）
- [ ] tmux 真实 DeepSeek 完成多步只读和经确认写改，显示 Token 且无密钥泄漏。
- [ ] README 和界面解释模式、上限、并发、停止与 Token 语义。（人工检查）
- [ ] 版本为 0.3.0，工作树干净且公开远端与本地一致。（Git 验证）
